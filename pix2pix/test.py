        # Updated section
        axes[0].set_title('Title for the First Plot')  # Add title for the first plot
        axes[0].plot(x1, y1)  # Your existing plot logic
        axes[0].set_xlabel('X-axis Label')
        axes[0].set_ylabel('Y-axis Label')
        axes[0].legend(['Legend Entry'])
        # Add other axes and logic as needed